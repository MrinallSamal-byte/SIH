<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Kinetic Zero design tokens, mirrored here (as plain XML colors) only for the
         pieces Android needs before Compose takes over: the launcher icon background
         and the pre-Compose window/splash background. All in-app UI color comes from
         ui/theme/Color.kt, not from this file - keep the two in sync by hand if you
         change the palette. -->
    <color name="kinetic_background">#FF121414</color>
    <color name="kinetic_primary">#FFFFFFFF</color>
    <color name="kinetic_sos_red">#FFE53935</color>
</resources>
