# Intentionally empty — deployment uses onnxruntime (see requirements-deploy.txt).
# This file only exists so the Render build command
#   pip install -r requirements-torch-cpu.txt --index-url https://download.pytorch.org/whl/cpu
# remains valid without pulling multi-GB CUDA wheels.
