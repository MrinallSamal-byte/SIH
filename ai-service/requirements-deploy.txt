# Deployment (Render.com) — inference-only dependencies.
# No PyTorch here: the model runs as ONNX via onnxruntime (~5x less RAM,
# fits Render's 512 MB free tier). requirements-torch-cpu.txt is kept empty
# so the Render build command stays valid.

fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.9
httpx==0.27.2
Pillow==11.1.0
piexif==1.1.3
imagehash==4.3.1
numpy==2.1.3
pydantic==2.10.3
pydantic-settings==2.6.1
huggingface_hub>=0.25
onnxruntime>=1.19
